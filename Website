index.html

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>PR Landscapes & Decorating</title>
  <meta name="description" content="PR Landscapes & Decorating — landscaping and decorating services." />
  <style>
    :root{
      --green-1: #2f8f3b;
      --green-2: #8dd26b;
      --orange: #ff8a2b;
      --blue: #2bb5e6;
      --bg: #ffffff;
      --muted: #55606a;
      --radius: 12px;
      --container: 1100px;
      font-family: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }
    *{box-sizing:border-box}
    body{margin:0;background:var(--bg);color:#123;line-height:1.45}
    .container{max-width:var(--container);margin:0 auto;padding:0 20px}
    .site-header{background:linear-gradient(180deg, rgba(47,143,59,0.06), transparent);padding:18px 0;position:sticky;top:0;z-index:50}
    .header-inner{display:flex;align-items:center;justify-content:space-between}
    .brand{display:flex;align-items:center;gap:12px}
    .logo{width:64px;height:64px;object-fit:contain;border-radius:8px}
    .brand-text h1{margin:0;font-size:18px;color:var(--green-1)}
    .tag{margin:0;font-size:13px;color:var(--muted)}
    .nav a{margin-left:14px;color:var(--muted);text-decoration:none;font-weight:600}
    .cta{background:var(--orange);color:white;padding:8px 12px;border-radius:8px}
    .hero{padding:48px 0;background:linear-gradient(135deg, rgba(47,143,59,0.04) 0%, rgba(255,138,43,0.02) 100%)}
    .hero-inner{display:flex;gap:32px;align-items:center;justify-content:space-between}
    .hero-text h2{font-size:30px;margin:0;color:#063018}
    .hero-text p{color:var(--muted)}
    .btn{display:inline-block;padding:10px 14px;border-radius:10px;background:var(--green-1);color:white;text-decoration:none;margin-top:12px}
    .btn.primary{background:var(--orange)}
    .section{padding:44px 0}
    .services .cards{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin-top:18px}
    .card{background:linear-gradient(180deg, rgba(255,255,255,0.9), #fff);border-radius:12px;padding:18px;box-shadow:0 6px 18px rgba(6,48,24,0.04)}
    .card h4{margin:0;color:var(--green-1)}
    .gallery .grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-top:12px}
    .img.placeholder{background:linear-gradient(90deg,#f6f9f6,#eff7ee);display:flex;align-items:center;justify-content:center;height:140px;border-radius:8px;color:var(--muted)}
    .contact-inner{display:flex;gap:26px}
    .contact-form{flex:1}
    .contact-info{flex:0 0 280px;background:#fbfbfb;padding:16px;border-radius:12px}
    label{display:block;margin-bottom:10px;font-size:13px}
    input,textarea{width:100%;padding:10px;border-radius:8px;border:1px solid #e6e6e6;margin-top:6px}
    .form-note{margin-top:10px;color:var(--muted)}
    .site-footer{padding:20px 0;background:#fafafa;color:var(--muted);text-align:center}
    .small{font-size:12px;color:#9aa3a0;margin-top:6px}
    @media(max-width:900px){
      .hero-inner{flex-direction:column;align-items:flex-start}
      .services .cards{grid-template-columns:1fr}
      .gallery .grid{grid-template-columns:1fr 1fr}
      .contact-inner{flex-direction:column}
      .nav{display:none}
    }
  </style>
</head>
<body>
  <header class="site-header">
    <div class="container header-inner">
      <div class="brand">
        <!-- data:image/png;base64,iVBORw0KGgo... -->
        <img src="data:image/png;base64,PUT-YOUR-LOGO-CODE-HERE" 
             alt="PR Landscapes & Decorating logo" 
             class="logo" />
        <div class="brand-text">
          <h1>PR Landscapes & Decorating</h1>
          <p class="tag">Landscaping • Decorating • Home Refresh</p>
        </div>
      </div>
      <nav class="nav">
        <a href="#services">Services</a>
        <a href="#gallery">Gallery</a>
        <a href="#about">About</a>
        <a class="cta" href="#contact">Get a Quote</a>
      </nav>
    </div>
  </header>

  <main>
    <section class="hero" id="home">
      <div class="container hero-inner">
        <div class="hero-text">
          <h2>We bring gardens & interiors to life</h2>
          <p>Playful, professional landscaping and decorating — tailored to your home.</p>
          <a class="btn" href="#contact">Request a free quote</a>
        </div>
      </div>
    </section>

    <section id="services" class="section services">
      <div class="container">
        <h3>Our Services</h3>
        <div class="cards">
          <article class="card"><h4>Garden design & maintenance</h4><p>Planting, turf, borders, seasonal care and makeovers.</p></article>
          <article class="card"><h4>Exterior hardscaping</h4><p>Patios, pathways, edging and outdoor living features.</p></article>
          <article class="card"><h4>Interior decorating</h4><p>Painting, colour consultations, wallpaper, and small refurbs.</p></article>
        </div>
      </div>
    </section>

    <section id="gallery" class="section gallery">
      <div class="container">
        <h3>Before & After</h3>
        <div class="grid">
          <div class="img placeholder">Image 1</div>
          <div class="img placeholder">Image 2</div>
          <div class="img placeholder">Image 3</div>
        </div>
      </div>
    </section>

    <section id="about" class="section about">
      <div class="container">
        <h3>About PR Landscapes & Decorating</h3>
        <p>Local, friendly, and detail-oriented. We combine landscaping expertise with decorating flair to create beautiful, usable spaces.</p>
      </div>
    </section>

    <section id="contact" class="section contact">
      <div class="container contact-inner">
        <div class="contact-form">
          <h3>Get a free quote</h3>
          <form id="quoteForm">
            <label>Name <input type="text" name="name" required></label>
            <label>Email <input type="email" name="email" required></label>
            <label>Phone <input type="tel" name="phone"></label>
            <label>Brief details <textarea name="message" rows="4" required></textarea></label>
            <button type="submit" class="btn primary">Send request</button>
            <p class="form-note">Or call: <strong>01234 567890</strong></p>
          </form>
        </div>
        <div class="contact-info">
          <h4>Services in:</h4>
          <p>Local area — call for availability & free site visit.</p>
          <div class="hours"><strong>Hours</strong><p>Mon–Sat: 8am–6pm</p></div>
        </div>
      </div>
    </section>
  </main>

  <footer class="site-footer">
    <div class="container">
      <p>© <span id="year"></span> PR Landscapes & Decorating — All rights reserved.</p>
      <p class="small">Designed for a playful brand. Replace images and text to personalise.</p>
    </div>
  </footer>

  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
    document.querySelectorAll('a[href^="#"]').forEach(a=>{
      a.addEventListener('click', e=>{
        const target = document.querySelector(a.getAttribute('href'));
        if(target){ e.preventDefault(); target.scrollIntoView({behavior:'smooth'}); }
      });
    });
    document.getElementById('quoteForm').addEventListener('submit', function(e){
      e.preventDefault();
      const name = this.name.value;
      alert('Thanks, ' + (name||'there') + '! Your request was received. We will contact you soon.');
      this.reset();
    });
  </script>
</body>
</html>
